# 検証状況

**いま何が確かめられているか。** [thesis.md](thesis.md) はここを出典として実測を引く。

対象は Rulealize の 6 ルールセット (reversi / roster / blackjack / chess / shogi /
deploy)。`deploy` はプラグインが NuGet に無いので静的解析のみ。

実測は思想を確かめるための使い捨ての試作で行った。**試作はこのリポジトリに入れていない。**
Ruledger の実装はこれから書く。下の「道具側」で名前を挙げているものは、その試作の中にある。

## 実測で確かめた

| thesis | 主張 | 何を測ったか |
|---|---|---|
| §4.2 | 出力は観測できること三つを状態ごとに並べたもの | 5 ルールセットを歩いて出力を得た |
| §4.3 | 網羅の対象も観測できることに限られる | 組・終局・`result` の三つで数えられた |
| §4.3.1 | 終局を観測したければ終局まで歩くしかない | 幅優先は深さ 7 で終局 0 件。歩き切れば深さ 63、終局 5,378 件 |
| §4.3.2 | 同定に使う状態は機械的に決まる | 6 ルールセットに走査を当て、`roster` と `deploy` の `log` が潰す側に落ちた。改変版二つで、名前でなく構造に反応することも確認 |
| §4.3.2 | 潰さないと持ち越しが壊れる | roster のガードを一つきつくした版で、潰せば 3/20、潰さなければ 0/20 |
| §4.5 | 差分は変更の大きさに比例する | 勝敗の入れ替えは状態 3,000 件すべて共通で 844 件の `result` だけ変化。置ける条件の変更は共通 1 件 |
| §4.5 | 差分は揺れない | 同じ設定で繰り返し、同じ値 |
| §5.3 | 粒度を下げて出し直せる | 予算を 3,000→100 に変えても編集 20 件が残る。落ちるのは予算より後ろの分だけ |
| §6.7 | 編集はルールセットの構造を指さない | 定義名を変え引数を並べ替えても 20/20 |
| §6.7 | 観測結果を変えない変更では一件も失われない | reversi / roster / chess / shogi の 8 通りで確認 |
| §6.7 | 潰れた鍵に貼った編集は、その鍵を持つ全部に当たる | 一件の編集が 1,001 個の状態で発火した |
| §4.3.3 | 予算を上げても観測が増えないことがある | chess は 20,000→200,000 で組が 1,164 のまま |

## 論証で足りるもの — 測っていないが、測ったものから従う

- **§4.3.1 歩き方の設定を変えても差分と持ち越しは成立する。** 両版を同じ設定で歩くかぎり
  同じ状態を同じ順で訪れるからである。**差分と持ち越しを測ったのは既定の設定だけで、
  他の設定では測っていない。**
- **§4.4 同じ版に対して実行すれば必ず通る。** 観測がランタイムから一意に決まり、歩き方が
  決定的だから。決定性のほうは実測してある (上の「差分は揺れない」)。

## 確かめていない

**思想側。**

- **§6.7 の穴。** 状態の項目が増減すると持ち越しの鍵が変わる。**パスで同定すれば消える
  かもしれない。** 出力は既に状態をパスで名付けている (`#1 = #0 + place(at: c4)`) のに、
  同定には状態そのものを選んでいて、来かたを検討していない。ただし重複排除とは噛み合わない
- **同定は重複排除だけのために要るのか。** 持ち越しと差分がパスに移るなら、§4.3.2 の
  走査が要るのは歩行の重複排除だけになる
- **§6.7 穴が開くことは前もって分かる。** §4.3.2 の走査で項目の増減は出るが、
  「出し直す前に警告する」を実際にやってはいない
- **§2.5 Ruledger はルールセットの穴を拾う。** shogi に千日手の規定が無いことは
  `terminal.when` を読めば分かるが、**それを報告する道具はいま無い** (`derive.py` は
  削除した)。歩いて届かなかったことを根拠にしてはいけない、は Phase 4 で確定した

**道具側。思想は待っていない。**

- `influence.py` を C# に移す。いま人が出力を読んで `RULEDGER_IGNORE` に打ち直しており、
  §4.3.2 の「機械的に決まる」が人を経由している
- shogi は一状態 50〜65ms で、20,000 状態が 20 分を超える (`GetValidInputs` が重い)
- 候補提示の形 (§6.7)

## 確かめる対象ではないもの

- **§4.4 オラクルは人の意図であり、システムの外にある** — **定義である。** Ruledger が何を
  しないかを決めている。真偽を測る対象ではない
- **§1 / §2 実装は要求より多くを決める** — **前提である。** これが偽なら企画ごと無用に
  なるが、それはこの文書が扱う話ではない
